<table>
  <tr>
    <th>Cats</th>
  </tr>
  <%= @cats.each do |cat| %>
    <tr>
      <td><%= cat.name %></td>
    </tr>
  <% end %>
</table>

<table>
  <tr>
    <th>Users</th>
  </tr>
  <%= @users.each do |user| %>
    <tr>
      <td><%= user.username %></td>
      <td><%= user.email %></td>
      <td><%= user.age %></td>
    </tr>
  <% end %>
</table>

<table>
  <tr>
    <th>Todos</th>
  </tr>
  <%= @todos.each do |todo| %>
    <tr>
      <td><%= todo.tasks %></td>
      <td><%= todo.finished %></td>
    </tr>
  <% end %>
</table>